# holochain-forum-theme

The Discourse theme for forum.holochain.org

## Why isn't there anything in here?

There are two branches that hold actual stuff: `light` and `dark`. `dark` is a fork of `light`. I did it this way so it'd be easy to keep the two in sync using nothing but Git tools.